{
  "builds": [
    {
      "src": "quote/package.json",
      "use": "@vercel/next"
    }
  ],
  "buildCommand": "cd quote && npm install && npm run build",
  "installCommand": "cd quote && npm install"
}